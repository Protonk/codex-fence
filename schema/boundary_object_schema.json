{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Boundary schema descriptor (v1)",
  "description": "Descriptor pattern for boundary-event schemas. Instances declare a schema key and embed the boundary-event JSON Schema used to validate emitted records.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "key",
    "boundary_schema"
  ],
  "properties": {
    "key": {
      "type": "string",
      "description": "Boundary schema key (e.g., cfbo-v1) used by emitted records as schema_key.",
      "pattern": "^[A-Za-z0-9_.-]+$"
    },
    "title": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "labels": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "boundary_schema": {
      "type": "object",
      "description": "Embedded JSON Schema describing boundary-event records for this key."
    }
  }
}
